AppContainer:

<AppContainer>:
    id: app_container
    canvas.before:
        Color:
            rgba: 1, 1, 1, 1
        Rectangle:
            pos: self.pos
            size: self.size

    ScreenManager:
        id: projectManager
        HomeScreen:

    MapUi:
        id: map
        map_size: dp(300)
        size_hint: None, None
        pos_hint: {'right': 1, 'bottom': 1}

    LoadingSpinner:
        id: loadingSpinner

<HomeScreen@Screen>:
    name: 'home_screen'
    BoxLayout:
        size_hint: 1, 1
        orientation: 'vertical'
        Filler:
        SimpleButton:
            primary_state_color: app.rgb_formatter([2, 153, 139, 255], factor=0.35, darken=True)
            pos_hint: {'center_x': .5}
            label_text: 'MD Icons Viewer'
            on_release: app.nav_to_project('MdIconsViewerScreen')
        Filler:
        SimpleButton:
            primary_state_color: app.rgb_formatter([2, 153, 139, 255], 0.35, darken=True)
            pos_hint: {'center_x': .5}
            label_text: 'Time Calculator'
            on_release: app.nav_to_project('TimeCalculatorScreen')
        Filler:
        SimpleButton:
            primary_state_color: app.rgb_formatter([2, 153, 139, 255], 0.35, darken=True)
            pos_hint: {'center_x': .5}
            label_text: 'Unit Converter'
            on_release: app.nav_to_project('UnitConverterScreen')
        Filler:
        SimpleButton:
            primary_state_color: app.rgb_formatter([2, 153, 139, 255], 0.35, darken=True)
            pos_hint: {'center_x': .5}
            label_text: 'Countries'
            on_release: app.nav_to_project('CountriesMainScreen')
        Filler:
        SimpleButton:
            primary_state_color: app.rgb_formatter([2, 153, 139, 255], 0.35, darken=True)
            pos_hint: {'center_x': .5}
            label_text: 'Color Picker'
            on_release: app.nav_to_project('ColorPickerScreen')
        Filler: